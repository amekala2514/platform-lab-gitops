apiVersion: v1
kind: Service
metadata:
  name: event-generator
  namespace: data-apps
  labels:
    app: event-generator
spec:
  type: ClusterIP
  selector:
    app: event-generator
  ports:
    - name: http
      port: 80
      targetPort: http
