apiVersion: v1
kind: Service
metadata:
  name: iceberg-rest
  namespace: data-storage
  labels:
    app.kubernetes.io/name: iceberg-rest
spec:
  selector:
    app.kubernetes.io/name: iceberg-rest
  ports:
    - name: http
      port: 8181
      targetPort: 8181
