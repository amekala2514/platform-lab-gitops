apiVersion: v1
kind: Service
metadata:
  name: platform-api
  namespace: default
  labels:
    app: platform-api
spec:
  type: ClusterIP
  selector:
    app: platform-api
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      protocol: TCP
